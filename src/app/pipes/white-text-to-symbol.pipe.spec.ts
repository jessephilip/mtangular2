import { WhiteTextToSymbolPipe } from './white-text-to-symbol.pipe';

describe('WhiteTextToSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new WhiteTextToSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
