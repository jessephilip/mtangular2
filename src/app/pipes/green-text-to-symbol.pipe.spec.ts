import { GreenTextToSymbolPipe } from './green-text-to-symbol.pipe';

describe('GreenTextToSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new GreenTextToSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
