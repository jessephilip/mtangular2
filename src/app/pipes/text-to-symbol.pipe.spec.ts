import { TextToSymbolPipe } from './text-to-symbol.pipe';

describe('TextToSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new TextToSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
