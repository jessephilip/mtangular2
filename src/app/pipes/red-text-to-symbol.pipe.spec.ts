import { RedTextToSymbolPipe } from './red-text-to-symbol.pipe';

describe('RedTextToSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new RedTextToSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
