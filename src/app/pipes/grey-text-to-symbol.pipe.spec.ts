import { GreyTextToSymbolPipe } from './grey-text-to-symbol.pipe';

describe('GreyTextToSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new GreyTextToSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
